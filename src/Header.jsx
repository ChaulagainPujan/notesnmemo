import React from "react";
import CreateIcon from '@material-ui/icons/Create';

function Header()
{
    return(
    <div className="header">
        <CreateIcon className="create" />Notes and Memos
    </div>
      );
}

export default Header;